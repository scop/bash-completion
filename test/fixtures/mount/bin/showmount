#!/bin/sh

if [ "$1" = -e ] && [ "$2" = mocksrv ]; then
    echo "Header line"
    echo "/test/path"
    echo "/test/path2"
    echo "/second/path"
    exit 0
fi

echo "Usage: 'showmount -e mocksrv'; nothing else works."
exit 1
