source lib/library.exp


proc unit_exit {} {
        # Exit bash
    send "\rexit\r"
}; # unit_exit()


proc unit_start {} {
    start_bash
    source_bash_completion
    init_tcl_bash_globals
}; # unit_start()


proc unit_version {} {
    puts "$::TESTDIR, bash-$::BASH_VERSION"
}
