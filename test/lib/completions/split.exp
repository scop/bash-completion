proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {split --help} "" "" "unsupported"]} {
    assert_complete_any "split --"
}; # if


sync_after_int


teardown
