proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


assert_complete_any "dd --"


sync_after_int


set test "option should be suffixed with ="
set cmd "dd if"
send "$cmd\t"
expect -ex "$cmd"
expect {
    -re "^=$" { pass $test }
    -re {^\\=$} { xfail $test }
}; # expect


sync_after_int


teardown
