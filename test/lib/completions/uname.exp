proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {uname --help} "" "" "unsupported"]} {
    assert_complete_any "uname --"
}; # if


sync_after_int


teardown
