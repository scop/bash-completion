proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {m4 --help} "" "" "unsupported"]} {
    assert_complete_any "m4 --"
}; # if


sync_after_int


teardown
