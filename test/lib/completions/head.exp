proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {head --help} "" "" "unsupported"]} {
    assert_complete_any "head --"
}; # if


sync_after_int


teardown
