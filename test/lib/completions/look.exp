proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {look foo | grep ^foo} words]} {
    assert_complete $words "look foo"
}; # if


sync_after_int


teardown
