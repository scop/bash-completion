proc setup {} {
    save_env
}


proc teardown {} {
    assert_env_unmodified
}


setup


assert_complete_any "unrar -"

assert_bash_exec ":>-a.rar"
assert_complete {-a.rar} "unrar x -x@ -- -"

sync_after_int


teardown
