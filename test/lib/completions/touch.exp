proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {touch --help} "" "" "unsupported"]} {
    assert_complete_any "touch --"
}; # if


sync_after_int


teardown
