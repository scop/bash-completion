proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {tr --help} "" "" "unsupported"]} {
    assert_complete_any "tr --"
}; # if


sync_after_int


teardown
