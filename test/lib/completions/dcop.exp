proc setup {} {
    save_env
}; # setup()


proc teardown {} {
    assert_env_unmodified
}; # teardown()


setup


if {[assert_exec {dcop} words]} {
    assert_complete $words "dcop "
}; # if


sync_after_int


teardown
